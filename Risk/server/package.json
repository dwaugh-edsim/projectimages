{
  "name": "risk-multiplayer-server",
  "version": "1.0.0",
  "description": "Real-time multiplayer server for the Risk board game (WebSocket). Hosts the static client and runs authoritative game state + heuristic AI opponents.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
